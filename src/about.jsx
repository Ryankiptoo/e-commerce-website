import React from 'react';

const about = () => {
    return (
        <div className='about'>
            <button onClick={() => open}>

            </button>
            
        </div>
    );
}

export default about;
